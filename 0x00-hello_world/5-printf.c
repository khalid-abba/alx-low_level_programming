#include <stdio.h>

/**
 *  * main - Entry point
 *   *
 *    * Description: Main function returns a string
 *     * Return: ALways 0 (success)
 *      */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
